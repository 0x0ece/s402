{
  "name": "s402",
  "version": "0.1.0",
  "private": true,
  "description": "Solana-based API payment solution with RFC 9421 HTTP message signatures",
  "packageManager": "pnpm@9.14.2",
  "scripts": {
    "test": "jest",
    "build": "cd packages/core && pnpm run build && cd ../express && pnpm run build && cd ../react && pnpm run build && cd ../../examples/webapp && pnpm run build && rm -rf ../../dist && cp -r dist/client ../../dist",
    "build:all": "pnpm -r build",
    "ci": "pnpm install && pnpm run build",
    "deploy": "wrangler deploy",
    "clean": "pnpm -r clean && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^22.19.11",
    "jest": "^29.7.0",
    "ts-jest": "^29.4.6",
    "typescript": "^5.9.3",
    "wrangler": "4.65.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
